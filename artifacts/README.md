# This directory holds serialized artifacts (FAISS index, GMM model, UMAP reducer).

# These are generated by the preprocessing and clustering scripts.

# Do not commit binary artifacts to git — rebuild with:

# python -m scripts.01_preprocess

# python -m scripts.02_cluster
